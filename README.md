# Blog

Using Emacs 26.3 to build the static blog.

- ```make clean``` -> ```make``` -> copy ```/org/index.html``` to main directory. DONE. publish.

## How to use
Must ```git clone``` to your home directory. Don't ask why. Just using ...

### Change the title

Edit the title in the file ```org/header.html```
